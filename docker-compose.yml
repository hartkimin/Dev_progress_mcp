version: '3.8'

services:
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    container_name: dev_progress_web
    ports:
      - "3002:3002"
    volumes:
      - ./:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3002
